[build]
  publish = "."
  functions = "netlify/functions"

# NOTE: CORS headers are intentionally NOT set globally here.
# Each function handles its own CORS, with origin-specific checks,
# so we never accidentally broadcast Access-Control-Allow-Origin: * for
# authenticated or quota-protected endpoints.
